<Page
    x:Class="Snake.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="Segoe UI Light"
    FontSize="48"
    FontWeight="Bold"
    DoubleTapped="Page_DoubleTapped"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SwapChainPanel x:Name="monogameRenderTarget"/>

        <TextBlock Padding="20 40" HorizontalAlignment="Left" VerticalAlignment="Top">Snake</TextBlock>

        <Grid Padding="20 40" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition MinWidth="100"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Foreground="Gray">Score</TextBlock>
            <TextBlock Grid.Row="0" Grid.Column="1" TextAlignment="Right" x:Name="score">0</TextBlock>
            <TextBlock Grid.Row="1" Grid.Column="0" Foreground="Gray">Highscore</TextBlock>
            <TextBlock Grid.Row="1" Grid.Column="1" TextAlignment="Right" x:Name="highscore">0</TextBlock>
        </Grid>
    </Grid>
</Page>
